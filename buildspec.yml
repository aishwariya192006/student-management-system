version: 0.2

phases:
  install:
    commands:
      - echo No install needed
  build:
    commands:
      - echo Build complete!

artifacts:
  files:
    - index.html
    - style.css
    - index.js
    - appspec.yml
    - scripts/**
  discard-paths: no
